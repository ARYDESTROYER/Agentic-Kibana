---
title: Agentic SOC 0.1.10
description: Stable-publication fixture portability release notes, signed-update artifacts, compatibility boundary, bootstrap, and operator verification for Agentic SOC 0.1.10.
---

# Agentic SOC 0.1.10

Agentic SOC **0.1.10** is the governed Stable-publication correction after the
immutable, non-installable `v0.1.9` attempt. Version 0.1.9 passed exact-tag CI,
built and signed all three public digest images, passed credential-free registry
verification, and generated, signed, and host-verified the canonical plan. The next
gate started the real constrained updater, but its read-only `/verification` bind
source was a runner-owned `mktemp` directory that the capability-dropped container
could not traverse. The workflow stopped before attestations, GitHub Release
publication, plan-asset publication, Stable convenience tags, documentation
publication, or application mutation.

Version 0.1.10 installs the two verification inputs with mode `0444`, makes their
temporary directory read-only and traversable with mode `0555`, and then mounts it
read-only into the updater. It adds fail-closed contract coverage for that exact
least-privilege ordering. It changes no application-state schema, updater protocol,
signed-plan schema, keyless publisher identity, process UID/GID, capability set,
trust predicate, or frozen base Compose file. It remains in documentation line
**0.1**.

!!! warning "Verify the immutable release before installation"

    A checkout, branch head, version string, candidate image, signed digest, or
    generated plan alone is not a Stable release. Version 0.1.10 becomes installable
    only when the annotated tag `v0.1.10` resolves to the accepted `main` commit,
    the complete fail-closed tag CI and release workflows pass, the public GitHub
    Release contains exactly the canonical plan and bundle, every pinned digest is
    anonymously readable and signature-verified, and canonical bootstrap/update
    acceptance succeeds. If any evidence is missing, keep the existing supported
    deployment unchanged.

    The immutable `v0.1.9` tag and signed digest objects are partial historical
    evidence only. Its GitHub Release and canonical asset pair do not exist. Never
    move, reuse, repair, install, or bootstrap from that tag.

## What changed after 0.1.9

Stable publication now prepares the constrained updater's verification bind source
in this order:

1. create a private temporary directory;
2. install `upgrade-plan.json` and `upgrade-plan.sigstore.json` at their exact names
   with mode `0444`;
3. remove directory write access and permit traversal with mode `0555`; and
4. start the shipping updater with the directory mounted read-only at
   `/verification`.

The container explicitly checks both files are readable before invoking
`cosign verify-blob`. Release-policy tests require every operation exactly once and
in order. They reject recursive or world-writable permission changes, ownership
changes, source `cp`, runtime entrypoint/environment overrides, Docker-socket access,
or `continue-on-error`. The fixture is readable without widening the shipping
process: the updater still runs as UID/GID `0:10001` with `cap_drop: ALL`,
`read_only: true`, and `no-new-privileges`.

The writable Sigstore trust-root correction introduced in the non-installable 0.1.9
source remains:

```text
TUF_ROOT=/var/lib/agentic-soc-updater/sigstore-root
```

That path is on the existing updater-state volume. Bootstrap continues to replace an
inspectable idle supervisor unless its reported updater version exactly matches the
target. Active, unreadable, malformed, or otherwise unsafe state fails closed.

The updater remains the only host-authorized component. The backend and Console
never receive the Docker socket, registry credentials, host commands, arbitrary
artifact coordinates, Compose text, or migration authority.

## Carried application scope

This patch carries the complete accepted 0.1 application scope described by the
[0.1 release line](0.1.md), including:

- vendor-neutral OCSF ingest, deterministic correlation/risk/decision boundaries,
  and one cost-ledgered LLM gateway;
- the Security Command Center, canonical split-pane Case Manager, durable Workspace
  Chat, collaboration, and version-matched installed Help Center;
- governed runbook/playbook, memory, tuning, rules, approval, enrichment, and export
  workflows; and
- signed supervised updates for the supported canonical PostgreSQL Compose profile.

The model, memory, runbook, and playbook layers remain advisory. Deterministic code
continues to own the final close/escalate route.

## Immutable release artifacts

The canonical repository is `ARYDESTROYER/Agentic-Kibana`. After successful
publication, the workflow exposes these convenience tags:

```text
ghcr.io/arydestroyer/agentic-kibana/backend:v0.1.10
ghcr.io/arydestroyer/agentic-kibana/webui:v0.1.10
ghcr.io/arydestroyer/agentic-kibana/updater:v0.1.10
```

Convenience tags are not update authority. The signed plan contains only immutable
digest references:

```text
ghcr.io/arydestroyer/agentic-kibana/backend@sha256:<digest>
ghcr.io/arydestroyer/agentic-kibana/webui@sha256:<digest>
ghcr.io/arydestroyer/agentic-kibana/updater@sha256:<digest>
```

The public GitHub Release must contain exactly these two assets:

```text
upgrade-plan.json
upgrade-plan.sigstore.json
```

Their canonical URLs are:

```text
https://github.com/ARYDESTROYER/Agentic-Kibana/releases/download/v0.1.10/upgrade-plan.json
https://github.com/ARYDESTROYER/Agentic-Kibana/releases/download/v0.1.10/upgrade-plan.sigstore.json
```

The expected keyless Sigstore certificate identity and OIDC issuer are:

```text
https://github.com/ARYDESTROYER/Agentic-Kibana/.github/workflows/release.yml@refs/tags/v0.1.10
https://token.actions.githubusercontent.com
```

The workflow identity signs each image digest and the canonical release plan. The
updater verifies the certificate identity, issuer, repository, annotated tag,
dereferenced tag commit, release version, compatibility envelope, and every exact
component digest.

## Signed plan contract

The plan is canonical JSON data, never a script or Compose fragment. Publication
uses schema 1 with concrete 0.1.10 identity:

```json
{
  "schema_version": 1,
  "product": "agentic-soc",
  "release": {
    "version": "0.1.10",
    "tag": "v0.1.10",
    "channel": "stable",
    "commit_sha": "<40-character lowercase tag commit SHA>",
    "published_at": "<timezone-qualified ISO-8601 timestamp>"
  },
  "compatibility": {
    "from": {"min_inclusive": "0.1.1", "max_exclusive": "0.2.0"},
    "state_backends": ["postgres"],
    "state_schema": 1,
    "compose_sha256": "e3f7ecbb0f749cc9d88f4392c58c9a63ddbd064e80ecde9f21fe9de199086fd4",
    "minimum_updater_protocol": 1,
    "backup_required": true,
    "migration": {"strategy": "none"}
  },
  "components": {
    "updater": {
      "image": "ghcr.io/arydestroyer/agentic-kibana/updater@sha256:<digest>",
      "digest": "sha256:<same updater digest>",
      "labels": {
        "org.opencontainers.image.version": "0.1.10",
        "org.opencontainers.image.revision": "<same tag commit SHA>",
        "org.opencontainers.image.source": "https://github.com/ARYDESTROYER/Agentic-Kibana",
        "dev.tlsoc.release.channel": "stable",
        "io.agentic-soc.state.schema": "1"
      }
    },
    "backend": {
      "image": "ghcr.io/arydestroyer/agentic-kibana/backend@sha256:<digest>",
      "digest": "sha256:<same backend digest>",
      "labels": {
        "org.opencontainers.image.version": "0.1.10",
        "org.opencontainers.image.revision": "<same tag commit SHA>",
        "org.opencontainers.image.source": "https://github.com/ARYDESTROYER/Agentic-Kibana",
        "dev.tlsoc.release.channel": "stable",
        "io.agentic-soc.state.schema": "1"
      }
    },
    "webui": {
      "image": "ghcr.io/arydestroyer/agentic-kibana/webui@sha256:<digest>",
      "digest": "sha256:<same webui digest>",
      "labels": {
        "org.opencontainers.image.version": "0.1.10",
        "org.opencontainers.image.revision": "<same tag commit SHA>",
        "org.opencontainers.image.source": "https://github.com/ARYDESTROYER/Agentic-Kibana",
        "dev.tlsoc.release.channel": "stable",
        "io.agentic-soc.state.schema": "1"
      }
    }
  },
  "rollout": {
    "backend_timeout_seconds": 300,
    "webui_timeout_seconds": 180,
    "observation_seconds": 30
  }
}
```

The updater alone derives the release-specific Compose override after verifying the
plan and image signatures. The plan cannot provide service names, paths, host
commands, credentials, infrastructure images, arbitrary registries/URLs, Compose
text, or migrations. The generated override cannot mutate the frozen base.

## Compatibility and migration

The supervised path supports only the canonical single-replica standalone Compose
deployment with PostgreSQL-owned state, canonical project/service/container/network
names, the canonical PostgreSQL volume, durable secrets,
`TLSOC_AUTH_ENABLED=true`, and updater protocol 1.

The frozen `deploy/docker-compose.agnostic.yml` SHA-256 remains:

```text
e3f7ecbb0f749cc9d88f4392c58c9a63ddbd064e80ecde9f21fe9de199086fd4
```

Release-specific version and digest pins exist only in the signed generated
override. A base-file change requires a new protocol and manual bootstrap; it cannot
be hidden in a patch plan.

SQLite, Elasticsearch-owned state, external PostgreSQL, Kubernetes, the legacy ELK
merge, horizontal replicas, modified Compose topology, missing durable secrets, or
unsupported migrations remain **Manual upgrade required**. PostgreSQL and Redis
infrastructure remain operator-managed.

There is no application-state schema migration in 0.1.10. A verified PostgreSQL
dump is mandatory before switching. Automatic rollback restores prior application
image pins; database restore remains an explicit break-glass operation because an
automatic restore could discard valid writes.

## Bootstrap and upgrade boundary

For a canonical deployment running supported Stable `v0.1.1`, use a clean checkout
of the strictly newer, fully published `v0.1.10` tag as the one-time host-authorized
bootstrap transport:

```bash
git fetch --tags origin
git switch --detach v0.1.10
test "$(git cat-file -t v0.1.10)" = tag
test "$(git rev-parse 'v0.1.10^{commit}')" = "$(git rev-parse HEAD)"
test -z "$(git status --porcelain --untracked-files=all)"

# .env must contain durable TLSOC_PG_PASSWORD, TLSOC_AUTH_ENABLED=true,
# TLSOC_AUTH_JWT_SECRET with at least 32 characters, and:
# AGENTIC_SOC_UPDATE_REPOSITORY=ARYDESTROYER/Agentic-Kibana
./scripts/bootstrap-updater.sh
```

Bootstrap verifies the exact public Release before installing or reconciling the
private supervisor and applying its strictly newer plan. If an earlier failed
attempt left an inspectable idle supervisor whose version differs from 0.1.10,
bootstrap preserves durable state and replaces it with the exact 0.1.10 updater.
It does not reuse a mismatched image merely because it reports protocol 1. Active,
unreadable, or invalid supervisor state still fails closed.

After a successful transition, use `./scripts/agentic-soc-compose.sh` for every
manual lifecycle operation so the active digest override and lifecycle lock remain
in effect.

Source-built/Testing 0.1.3 or 0.1.9 deployments and the immutable 0.1.4 through
0.1.9 failed/bootstrap-blocked publication records cannot be relabelled Stable.
Reconcile them according to their actual installed state. A deployment already
running 0.1.10 cannot bootstrap from the same 0.1.10 plan because the updater
requires a strictly newer target.

Read [Upgrades](../operations/upgrades.md) and [Known
limitations](known-limitations.md) before rehearsing the transition.

## Independent artifact verification

Verify that the remote exposes both the annotated tag object and its dereferenced
commit:

```bash
git ls-remote --tags https://github.com/ARYDESTROYER/Agentic-Kibana.git \
  refs/tags/v0.1.10 'refs/tags/v0.1.10^{}'
```

The command must return two different 40-character IDs: the tag object and the
accepted `main` commit.

Verify the canonical plan with a reviewed cosign installation:

```bash
tag=v0.1.10
repo=ARYDESTROYER/Agentic-Kibana
identity="https://github.com/${repo}/.github/workflows/release.yml@refs/tags/${tag}"
base="https://github.com/${repo}/releases/download/${tag}"

curl --fail --location --output upgrade-plan.json "${base}/upgrade-plan.json"
curl --fail --location --output upgrade-plan.sigstore.json \
  "${base}/upgrade-plan.sigstore.json"

cosign verify-blob \
  --bundle upgrade-plan.sigstore.json \
  --certificate-identity "${identity}" \
  --certificate-oidc-issuer https://token.actions.githubusercontent.com \
  upgrade-plan.json
```

Verify every immutable image signature and anonymous registry read with a fresh
Docker configuration so cached credentials cannot satisfy the public-access test:

```bash
anonymous_docker_config="$(mktemp -d)"
for component in backend webui updater; do
  image="$(jq -er --arg component "${component}" \
    '.components[$component].image' upgrade-plan.json)"
  cosign verify \
    --certificate-identity "${identity}" \
    --certificate-oidc-issuer https://token.actions.githubusercontent.com \
    "${image}"
  DOCKER_CONFIG="${anonymous_docker_config}" docker buildx imagetools inspect \
    "${image}" >/dev/null
done
```

The workflow also performs credential-free HTTP verification of every exact index,
child manifest, and config. All three GHCR packages must be **Public**; image push
alone does not change package visibility.

## Release acceptance

Publication requires the fail-closed `CI passed` aggregate on the frozen 0.1.10
candidate on `Testing`, the resulting accepted `main` commit, and the immutable
`v0.1.10` tag. The tag workflow then requires:

1. annotated tag and dereferenced commit equal the accepted `main` SHA;
2. all three dual-platform images build with exact Stable labels and signatures;
3. every image digest is anonymously readable and content-verified;
4. the plan passes schema, compatibility, digest, and Sigstore checks;
5. the real updater verifies the plan inside the production-constrained runtime,
   including readable immutable verification inputs and writable TUF state;
6. attestations complete;
7. the private draft contains exactly the canonical notes, plan, and bundle;
8. downloaded assets byte-match and signature-verify before one atomic public
   transition; and
9. Stable convenience tags are created only after Release publication.

Canonical PostgreSQL Compose acceptance must then bootstrap from the supported
baseline, retain the verified backup and durable receipt, switch to the exact plan
digests, and pass the post-update Console browser matrix. Never move, reuse, repair,
or republish a tag. Any later defect receives a newer SemVer.

## Exact Stable build identity

Resolve every Stable stamp from the exact annotated tag on the accepted `main`
commit. Never substitute a branch head, local clock, or unknown value:

```bash
git switch main
git pull --ff-only origin main
git fetch --tags origin

release_version="$(tr -d '[:space:]' < VERSION)"
test "${release_version}" = "0.1.10"
release_sha="$(git rev-parse "v${release_version}^{commit}")"
test "${release_sha}" = "$(git rev-parse HEAD)"
release_date="$(git show -s --format=%cI "${release_sha}")"

export TLSOC_VERSION="${release_version}"
export TLSOC_RELEASE_CHANNEL=stable
export TLSOC_BUILD_SHA="${release_sha}"
export TLSOC_BUILD_DATE="${release_date}"
```

Backend, Console, updater, `/release.json`, OCI labels, signed plan, annotated tag
`v0.1.10`, and GitHub Release must resolve to one version and commit. Any mismatch
fails safe to Testing and cannot authorize installation.

## Immutable annotated-tag checklist

Create the tag only after the clean candidate passes on `Testing`, promotion passes
on the resulting `main` commit, and the final tree is frozen:

```bash
test "$(tr -d '[:space:]' < VERSION)" = "0.1.10"
test -z "$(git status --porcelain)"
git tag -a v0.1.10 \
  -m "Agentic SOC 0.1.10" \
  -m "Stable release-fixture portability correction: immutable readable verification inputs under the constrained updater. Detailed notes: docs/releases/0.1.10.md; CHANGELOG.md."
test "$(git cat-file -t v0.1.10)" = tag
test "$(git rev-parse 'v0.1.10^{commit}')" = "$(git rev-parse HEAD)"
git show --no-patch --show-signature v0.1.10
git push origin v0.1.10
```

Do not create the tag before every candidate/main gate passes, and never force,
delete, move, recreate, or reassign it after push. See [Release channels and
versioning](channels.md) for the canonical promotion sequence.
